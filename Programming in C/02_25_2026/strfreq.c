//Q3) Character frequency count in a string ignore case sensitivity and ignore other characters
#include <stdio.h>
#include <ctype.h>

int main(){
	char s1[50];
	int i;
        printf("Enter your string: ");
        scanf("%s", s1);
	for(i=0; s1[i]!='\0'; i++){
		
	}
	return 0;
}

